// Deprecated: redirect handled via next.config redirects
// (no import needed)

// Redirect handled via next.config. This file now re-exports the builder page.
export { default } from '@/app/(dashboard)/builder/page';